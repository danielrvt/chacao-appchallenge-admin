'use strict';

angular.module('dndAdminTemplate')
  .controller('DashboardCtrl', ['$translate', '$rootScope', '$scope', '$state', '$sessionStorage', '$location', function ($translate, $rootScope, $scope, $state, $sessionStorage, $location) {

    //if (!$sessionStorage.session) $state.go('login');

  }]);
